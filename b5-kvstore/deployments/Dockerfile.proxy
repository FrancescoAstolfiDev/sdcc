FROM golang:1.22-alpine AS builder
WORKDIR /src
COPY . .
RUN go mod tidy && CGO_ENABLED=0 go build -o /out/client-proxy ./cmd/client-proxy

FROM alpine:3.20
COPY --from=builder /out/client-proxy /usr/local/bin/client-proxy
ENTRYPOINT ["/usr/local/bin/client-proxy"]
